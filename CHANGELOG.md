# Changelog

## 0.1.0

- Initial `ament_xmake` rule package scaffold.
- Added package manifest + marker install logic.
- Added generated `Config.cmake` export for downstream consumers.
- Added symlink-install aware file install behavior.
